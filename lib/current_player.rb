def turn_count(board)
counter = 0
board.each do |player|
 if {player = "X" || player = index "O"}
   counter += 1
  puts "#{counter}"
  end
end
turn_count(board)



def current_player(board, turn_count)

end
